# Dependencies
node_modules/

# Secrets
.env
.env.local
.env.*.local

# Recorded demo deliverable — attached on Discord/X, not stored in git.
# (Exceeds GitHub's 100 MB file limit.)
/Video Project 4.mp4
/*.mov
/*.mkv

# Demo narration audio — only needed to reproduce the recorded video, not the
# product. The web demo plays fine (silently) without it.
/assets/Audio clips/
/assets/demo-audio.mp3

# Internal planning / non-product docs (kept local, not published).
/AUDIT.md
/Idea.txt
/PROJECT_DESCRIPTION.md
/Hackathon rules.txt

# Logs / temp
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
